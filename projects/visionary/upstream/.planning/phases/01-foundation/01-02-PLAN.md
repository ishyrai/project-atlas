---
phase: "01-foundation"
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - server.js
  - sse.js
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06
must_haves:
  truths:
    - "POST /api/tasks creates a task in SQLite and returns the full task object with id"
    - "GET /api/tasks returns all tasks grouped or as flat array from database"
    - "PATCH /api/tasks/:id updates task fields and returns updated task"
    - "DELETE /api/tasks/:id removes task from database"
    - "GET /api/events opens an SSE stream that receives events when tasks are mutated"
    - "SSE sends keepalive comments every 30 seconds to prevent connection timeout"
    - "Activity log entries are created for every task mutation"
  artifacts:
    - path: "sse.js"
      provides: "SSE client manager, event bus, broadcast function"
      exports: ["broadcast", "handleSSE", "bus"]
    - path: "server.js"
      provides: "Full CRUD routes replacing placeholders, wired to SSE broadcast"
      contains: "stmts.insertTask"
  key_links:
    - from: "server.js"
      to: "sse.js"
      via: "require('./sse') for broadcast and handleSSE"
      pattern: "require.*sse"
    - from: "server.js"
      to: "db.js"
      via: "stmts.insertTask, stmts.updateTask, etc."
      pattern: "stmts\\."
    - from: "sse.js"
      to: "browser EventSource"
      via: "text/event-stream response at /api/events"
      pattern: "text/event-stream"
---

<objective>
Implement the full REST API for task CRUD and the SSE real-time push system. Replace the placeholder routes from Plan 01 with real database-backed handlers, and create the SSE broker that pushes events to connected browsers on every mutation.

Purpose: FOUND-05 (REST API with JSON responses) and FOUND-06 (SSE endpoint for real-time push) are the communication backbone. The frontend (Plan 03) needs working API endpoints to fetch and mutate data, and SSE to receive live updates.

Output: Fully functional task CRUD API and SSE event stream. Creating a task via POST shows up in GET, and any connected SSE client receives the event in real time.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01: db.js exports used by this plan -->
From db.js (created in Plan 01):
```javascript
module.exports = {
  db,    // better-sqlite3 Database instance
  stmts: {
    insertTask,        // INSERT with @title, @description, @status, @priority, @agent_id, @project_id, @sort_order
    getTasksByStatus,  // SELECT ... WHERE status = ?
    getTaskById,       // SELECT ... WHERE id = ?
    updateTask,        // UPDATE with @id, @title, @description, @status, @priority, @agent_id, @sort_order
    deleteTask,        // DELETE WHERE id = ?
    getAllTasks,        // SELECT all tasks with project join
    insertActivity,    // INSERT into activity_log with @event_type, @agent_id, @task_id, @project_id, @summary, @detail_json
    getRecentActivity, // SELECT ... ORDER BY created_at DESC LIMIT ?
  }
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SSE broker module with event bus and broadcast</name>
  <files>sse.js</files>
  <action>
  Create `sse.js` at project root:

  1. `const EventEmitter = require('node:events');`
  2. Create and export `bus = new EventEmitter()` -- the central event bus all mutations emit to.
  3. `const clients = new Set();` -- holds active SSE response objects.
  4. `let eventId = 0;` -- monotonic counter for SSE event IDs (in-memory; the activity_log table provides persistence).

  5. `function handleSSE(req, res)`:
     - Set headers: Content-Type text/event-stream, Cache-Control no-cache, Connection keep-alive, X-Accel-Buffering no
     - Write initial comment `:ok\n\n` to flush the connection
     - Send `retry: 3000\n\n` to set client reconnect interval
     - Add `res` to `clients` Set
     - Set up keepalive: `const keepAlive = setInterval(() => res.write(': keepalive\n\n'), 30000);`
     - Clean up on close: `req.on('close', () => { clients.delete(res); clearInterval(keepAlive); });`

  6. `function broadcast(eventType, data)`:
     - Increment `eventId`
     - Format SSE message: `id: ${eventId}\nevent: ${eventType}\ndata: ${JSON.stringify(data)}\n\n`
     - Iterate `clients` Set and write the message to each. Wrap each write in try/catch -- if write fails, delete that client from the Set (dead connection cleanup).

  7. Wire bus events to broadcast:
     - `bus.on('task:created', (d) => broadcast('task:created', d));`
     - `bus.on('task:updated', (d) => broadcast('task:updated', d));`
     - `bus.on('task:deleted', (d) => broadcast('task:deleted', d));`
     - `bus.on('activity:new', (d) => broadcast('activity:new', d));`

  8. Export: `module.exports = { bus, handleSSE, broadcast };`
  </action>
  <verify>
    <automated>cd ./ && node -e "const { bus, handleSSE, broadcast } = require('./sse'); console.log('bus type:', typeof bus.on); console.log('handleSSE type:', typeof handleSSE); console.log('broadcast type:', typeof broadcast); console.log('OK')"</automated>
  </verify>
  <done>sse.js exports bus (EventEmitter), handleSSE (SSE connection handler), and broadcast (push to all clients). Keepalive every 30s. Dead connection cleanup on write failure.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder API routes with real CRUD + SSE wiring in server.js</name>
  <files>server.js</files>
  <action>
  Modify `server.js` (created in Plan 01) to replace all placeholder routes with real implementations:

  1. Add require at top: `const { bus, handleSSE } = require('./sse');`

  2. Replace route handlers:

  **GET /api/tasks**:
  - Call `stmts.getAllTasks.all()`
  - Return `res.json({ tasks })`

  **POST /api/tasks**:
  - Read body via `readBody(req)`
  - Validate: title is required (non-empty string). Return 400 if missing.
  - Set defaults: status='todo', priority='medium', sort_order=0
  - Call `stmts.insertTask.run({ title, description: body.description || null, status: body.status || 'todo', priority: body.priority || 'medium', agent_id: body.agent_id || null, project_id: body.project_id || null, sort_order: body.sort_order || 0 })`
  - Get the created task: `const task = stmts.getTaskById.get(result.lastInsertRowid);`
  - Log activity: `stmts.insertActivity.run({ event_type: 'task.created', agent_id: null, task_id: task.id, project_id: task.project_id, summary: 'Task created: ' + task.title, detail_json: JSON.stringify(task) })`
  - Emit: `bus.emit('task:created', task)`
  - Emit: `bus.emit('activity:new', { event_type: 'task.created', summary: 'Task created: ' + task.title, task_id: task.id })`
  - Return `res.json({ task }, 201)`

  **PATCH /api/tasks/:id**:
  - Extract id from URL path segment
  - Read body
  - Get existing task: `stmts.getTaskById.get(id)`. Return 404 if not found.
  - Merge fields: only update fields present in body, keep existing values for others
  - Call `stmts.updateTask.run({ id, title, description, status, priority, agent_id, sort_order })`
  - Get updated task: `stmts.getTaskById.get(id)`
  - Log activity with appropriate event_type (use 'task.updated', or 'task.moved' if status changed)
  - Emit `bus.emit('task:updated', updatedTask)` and `bus.emit('activity:new', ...)`
  - Return `res.json({ task: updatedTask })`

  **DELETE /api/tasks/:id**:
  - Extract id, verify task exists (404 if not)
  - Get task before delete for activity log
  - Call `stmts.deleteTask.run(id)`
  - Log activity: 'task.deleted'
  - Emit `bus.emit('task:deleted', { id })` and `bus.emit('activity:new', ...)`
  - Return `res.json({ ok: true })`

  **GET /api/events**:
  - Call `handleSSE(req, res)` from sse.js

  **GET /api/activity**:
  - Parse query param `limit` (default 50, max 200)
  - Call `stmts.getRecentActivity.all(limit)`
  - Return `res.json({ activity })`

  **GET /api/notifications**:
  - Keep as placeholder for now (returns empty array) -- notifications are consumed in Phase 4
  - Return `res.json({ notifications: [] })`

  3. Make sure the try/catch wrapper from Plan 01 covers these new implementations. Log errors to console.error with the full error object.
  </action>
  <verify>
    <automated>cd ./ && timeout 15 node -e "
      const { spawn } = require('child_process');
      const s = spawn('node', ['server.js'], {cwd: './', stdio: 'pipe'});
      setTimeout(async () => {
        try {
          // Create a task
          const r1 = await fetch('http://127.0.0.1:3333/api/tasks', {
            method: 'POST',
            headers: {'Content-Type':'application/json'},
            body: JSON.stringify({title:'Test task', priority:'high'})
          });
          const c1 = await r1.json();
          console.log('Create:', r1.status, 'id:', c1.task?.id, 'title:', c1.task?.title);
          if(r1.status !== 201 || !c1.task?.id) process.exit(1);

          // Read tasks
          const r2 = await fetch('http://127.0.0.1:3333/api/tasks');
          const c2 = await r2.json();
          console.log('List:', r2.status, 'count:', c2.tasks?.length);
          if(c2.tasks?.length !== 1) process.exit(1);

          // Update task
          const r3 = await fetch('http://127.0.0.1:3333/api/tasks/' + c1.task.id, {
            method: 'PATCH',
            headers: {'Content-Type':'application/json'},
            body: JSON.stringify({status:'in_progress'})
          });
          const c3 = await r3.json();
          console.log('Update:', r3.status, 'status:', c3.task?.status);
          if(c3.task?.status !== 'in_progress') process.exit(1);

          // Delete task
          const r4 = await fetch('http://127.0.0.1:3333/api/tasks/' + c1.task.id, { method: 'DELETE' });
          const c4 = await r4.json();
          console.log('Delete:', r4.status, 'ok:', c4.ok);
          if(!c4.ok) process.exit(1);

          // Check activity log
          const r5 = await fetch('http://127.0.0.1:3333/api/activity');
          const c5 = await r5.json();
          console.log('Activity:', c5.activity?.length, 'entries');
          if(c5.activity?.length < 3) process.exit(1);

          // Check SSE endpoint opens
          const r6 = await fetch('http://127.0.0.1:3333/api/events');
          console.log('SSE content-type:', r6.headers.get('content-type'));
          if(!r6.headers.get('content-type')?.includes('text/event-stream')) process.exit(1);

          console.log('ALL OK');
        } catch(e) { console.error(e); process.exit(1); }
        finally { s.kill('SIGTERM'); setTimeout(() => process.exit(0), 500); }
      }, 1500);
    "</automated>
  </verify>
  <done>All task CRUD routes return real data from SQLite. Every mutation creates an activity log entry and emits SSE events. GET /api/events opens a persistent SSE stream. Validation returns 400 for missing title. 404 for non-existent task IDs.</done>
</task>

</tasks>

<verification>
1. POST /api/tasks with title creates a task and returns 201 with task object including id
2. GET /api/tasks returns the created task
3. PATCH /api/tasks/:id updates fields and returns updated task
4. DELETE /api/tasks/:id removes the task
5. GET /api/activity shows entries for all mutations
6. GET /api/events returns Content-Type text/event-stream
7. POST without title returns 400
8. PATCH/DELETE on non-existent id returns 404
</verification>

<success_criteria>
- Full task CRUD via REST API with JSON responses -- FOUND-05
- SSE endpoint pushes events to connected browsers on every mutation -- FOUND-06
- Activity log populated on every mutation
- Validation and error handling (400, 404, 500)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
